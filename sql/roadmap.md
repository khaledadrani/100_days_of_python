| Level                   | Who it’s for                                            | Core Topics & Concepts                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Example Projects / Assessments                                                                                                                                                                                                                                       |
| ----------------------- | ------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Beginner**            | Absolute newcomers to SQL or databases                  | **1. Core ideas**: What is a database, what is SQL, difference between DBMS, RDBMS, and file storage.<br> **2. Data concepts**: Tables, rows, columns, primary keys, data types.<br> **3. Basic operations (CRUD)**:<br> ‣ `SELECT`, `FROM`, `WHERE`<br> ‣ `INSERT`, `UPDATE`, `DELETE`<br> **4. Filtering and sorting**: `ORDER BY`, `DISTINCT`, `LIMIT`.<br> **5. Basic conditions**: `AND`, `OR`, `NOT`, comparison operators, `BETWEEN`, `LIKE`.<br> **6. Basic functions**: `COUNT`, `SUM`, `AVG`, `MIN`, `MAX`.<br> **7. Aliases and simple computed columns (`AS`)**.<br> **8. Basic `GROUP BY` and `HAVING`.**                                                                                                                         | 🗃️ Create a small “store” database with `customers`, `orders`, `products`.<br> Write 10 queries: top sales, most frequent customer, etc.<br> 💡 Task: “Find top 5 customers by total spending.”                                                                     |
| **Standard / Core SQL** | Learners comfortable writing basic queries              | **1. Joins (core skill)**:<br> ‣ `INNER`, `LEFT`, `RIGHT`, `FULL OUTER` joins.<br> ‣ Join conditions and table aliases.<br> **2. Subqueries (scalar, correlated, nested).**<br> **3. Set operations:** `UNION`, `INTERSECT`, `EXCEPT`.<br> **4. String, date, numeric functions.**<br> **5. NULL handling:** `IS NULL`, `COALESCE`, `NULLIF`.<br> **6. CASE expressions and conditional logic.**<br> **7. Basic schema design principles:** normalization (1NF–3NF).<br> **8. Constraints:** `PRIMARY KEY`, `UNIQUE`, `CHECK`, `FOREIGN KEY`.                                                                                                                                                                                                  | 🛍️ Build a mini e-commerce database.<br> Implement and query: total sales per category, per month, etc.<br> 🔍 Query challenge: “List customers who never made an order.”<br> 🧩 Small project: “Design and populate a school database.”                            |
| **Intermediate**        | Developers using SQL in production or analytics         | **1. Advanced joins and query optimization:** anti-joins, self-joins, lateral joins.<br> **2. Window (analytic) functions:**<br> ‣ `ROW_NUMBER()`, `RANK()`, `DENSE_RANK()`<br> ‣ `LAG()`, `LEAD()`, `FIRST_VALUE()`, `LAST_VALUE()`.<br> **3. Common Table Expressions (CTEs)** and recursive queries.<br> **4. Aggregation patterns (GROUPING SETS, ROLLUP, CUBE).**<br> **5. Transactions:** `BEGIN`, `COMMIT`, `ROLLBACK`.<br> **6. Isolation levels & concurrency basics.**<br> **7. Indexes:** how they work, when to use them.<br> **8. Views and materialized views.**<br> **9. Stored procedures and functions (basic scripting).**<br> **10. Working with JSON / XML columns (PostgreSQL, MySQL 8+).**                               | 📊 Build an **analytics dashboard backend** entirely in SQL.<br> e.g., Sales over time, cohort analysis, retention.<br> 🔁 Implement recursive CTE to show an org chart or category tree.<br> ⚙️ Create a function that validates and transforms data before insert. |
| **Advanced**            | Professionals dealing with complex data or optimization | **1. Query planning and optimization:** reading `EXPLAIN` / `EXPLAIN ANALYZE`, identifying slow queries.<br> **2. Index internals:** B-trees, hash indexes, composite indexes, covering indexes.<br> **3. Denormalization and data warehousing concepts.**<br> **4. Partitioning, sharding, and replication fundamentals.**<br> **5. Advanced transaction management:** locks, deadlocks, MVCC.<br> **6. Performance tuning:** caching, query rewriting, indexing strategies.<br> **7. Stored procedures, triggers, functions in depth.**<br> **8. Security:** roles, privileges, schema management, data masking.<br> **9. Automation and maintenance:** vacuuming, statistics, backups.<br> **10. Integrating SQL with Python / ETL tools.** | 🧠 Optimization challenge: Given a slow query, tune it using indexes and query rewrites.<br> 🧩 Build a **data warehouse star schema** (fact + dimensions).<br> 🔒 Set up roles and permissions for multi-tenant access.<br> 🔁 Create triggers for audit logs.      |
| **Expert**              | Engineers, DBAs, data architects, or researchers        | **1. Database architecture:** internals of query engines, parser, optimizer, executor.<br> **2. Storage engines (InnoDB, Postgres heap, columnar).**<br> **3. Advanced replication and clustering (Patroni, Galera, etc.).**<br> **4. Index design trade-offs, bloat management.**<br> **5. Distributed SQL systems:** CockroachDB, YugabyteDB, Citus.<br> **6. Temporal and time-travel databases.**<br> **7. Advanced data modeling (multi-tenant, event-sourced, graph extensions).**<br> **8. Internals of MVCC and WAL (write-ahead logs).**<br> **9. Tuning memory, I/O, and query concurrency.**<br> **10. Contributing to database engines or writing custom SQL functions in C/Rust.**                                                | 🧩 Build a **distributed query simulation** using Postgres + Citus.<br> ⚙️ Write a custom aggregate function in PL/pgSQL or C.<br> 🔍 Read and analyze the Postgres query planner source.<br> 📈 Design a **real-time analytics schema** for billions of records.    |
